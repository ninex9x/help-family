title = "help-family secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Browser storage namespace; it is an identifier, not a credential"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = ['''STORAGE_KEY\s*=\s*"cuidar-med-family-v1"''']
